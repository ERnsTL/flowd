# TCP chat/console server
# start using:
# bin/flowd -debug src/github.com/ERnsTL/flowd/examples/http-server.fbp
# send test request:
# wget --debug --post-data="my POST data" http://localhost:4000/

tcp(bin/tcp-server) OUT -> IN http(bin/http-parser) OUT -> IN Display(bin/display)
# NOTE: currently only displays each incoming HTTP request
'tcp4://localhost:4000' -> CONF tcp
